import { EventMap } from 'typed-emitter';
import EventEmitter from '../EventEmitter';
import { Event, AnyEvent } from '../eventTypes';
export declare type LogFn = (action: string, description: string) => void;
export declare const AnyValue: any;
export declare type Options = {
    useChaiExpect?: boolean;
    strict?: boolean;
    debug?: string;
};
export declare class EventListener {
    private receivedEvents;
    private expectedNextEvent;
    private log;
    private options;
    constructor(emitter: EventEmitter<EventMap>, eventType: Event, logFn: LogFn, options?: Options);
    private checkExpectation;
    private onEvent;
    waitForEvent: (expectedEvent: AnyEvent) => Promise<void>;
    getReceivedEvents(): AnyEvent[];
}
//# sourceMappingURL=EventListener.d.ts.map